# Vastu Expert API

C++ REST API using Crow framework.

## Endpoints
- GET  /             — health check
- POST /api/analyze  — analyze a house
- POST /api/compare  — compare two houses
- GET  /api/tip      — random Vastu tip

## Deploy on Render
Environment: Docker | Port: 8080
